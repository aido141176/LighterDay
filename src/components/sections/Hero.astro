---
// Hero.astro
const { headline, subtext } = Astro.props;
---

<section class="py-20 text-center">
  <h1 class="text-4xl font-bold">{headline}</h1>
  <p class="mt-4 text-lg">{subtext}</p>
</section>