SITE_URL=https://example.com
PUBLIC_TINA_CLIENT_ID=*** #Grab from TinaCloud app.tina.io
TINA_TOKEN=*** #Grab from TinaCloud app.tina.io
GF_CONSUMER_KEY=*** #Gravity Forms REST API key (WP: Forms > Settings > REST API) — also set in Vercel project env vars
GF_CONSUMER_SECRET=*** #Gravity Forms REST API secret — also set in Vercel project env vars